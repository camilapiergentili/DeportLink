package com.deportlink.deportlink.enums;

/** Origen de una fila de court_occupancy — permite liberar por origen, nunca por coordenadas. */
public enum OccupancySourceType {
    RESERVATION,
    CLASS_SESSION
}
