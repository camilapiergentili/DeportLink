# Loop: F16 — `AddScheduleUseCase` sin lock ni constraint permite duplicar horarios de una cancha/día y romper la disponibilidad/reserva de esa cancha con un 500 persistente

## Metadata
- **Finding:** F16
- **Auditoría de origen:** docs/software-review-2026-08-31.md (sección Findings, `### F16`, líneas ~131-155)
- **Objetivo:** Cerrar la condición de carrera en `AddScheduleUseCase.execute()` que permite que dos altas
  de horario concurrentes para la misma cancha inserten dos filas de `availability` para el mismo
  `(court_id, day_of_week)`, lo que rompe con un 500 (`IncorrectResultSizeDataAccessException`) toda
  consulta de disponibilidad, reserva o reprogramación para esa cancha/día hasta una corrección manual en
  la base.
- **Branch:** `fix/f16-add-schedule-lock` (creado off `development` @ `9a5e365`)
- **Estado actual:** RESOLVED
- **Iteración actual:** 1 / 5
- **Reanudaciones de TEST (esta iteración):** 0 / 2
- **Fase actual:** — (cerrado)
- **Reanudable en:** —
- **Última actualización:** 2026-08-31

## Scope
### IN_SCOPE
- `src/main/java/com/deportlink/deportlink/application/usecase/schedule/AddScheduleUseCase.java` — cambiar
  la lectura de `Court` de `findById` a `findByIdForUpdate` como primera operación de `execute()`.
- `src/test/java/com/deportlink/deportlink/usecase/reservation/AddScheduleConcurrencyTest.java` (nuevo) —
  test de concurrencia contra MySQL real (Testcontainers), mismo estilo que
  `BookReservationConcurrencyTest`/`BookReservationVsUpdateScheduleConcurrencyTest`, usando
  `ConcurrencyTestFixtures` ya existente en ese paquete.
- `src/test/java/com/deportlink/deportlink/usecase/schedule/AddScheduleUseCaseTest.java` (nuevo) — no
  existía ningún test unitario para `AddScheduleUseCase` antes de este loop; se agrega uno con Mockito que
  fija el orden de invocación (`findByIdForUpdate` antes que `findAllByCourtId`), mismo propósito que los
  tests Mockito documentan para `BookReservationUseCaseTest` (ver javadoc de
  `BookReservationConcurrencyTest`: el test Mockito prueba el orden, el de Testcontainers prueba que el
  lock realmente serializa).

### OUT_OF_SCOPE
- **El gap secuencial de `filterConflicts()`** (ver `RELATED_FINDING` arriba) — no se toca: requiere una
  decisión de producto que este loop no puede tomar por su cuenta.
- **`ReservationRepositoryAdapter.findActiveByCourtAndDay()` redirigiendo a la query muerta con lock**
  (la mitad de F4 que sigue sin aplicarse incluso en `fix/f4-schedule-concurrency-lock`) — finding
  distinto, no se toca.
- **Hacer que `ScheduleRepository.findByCourtIdAndDay` tolere más de una fila** (cambiarla de `Optional` a
  `List`/`findFirstBy...`) — mencionado como red de seguridad opcional en la ficha original de F16, pero
  es un cambio de contrato del puerto que afecta a los tres consumidores (`GetAvailableSlotsUseCase`,
  `BookReservationUseCase`, `RescheduleReservationUseCase`) y a `GetScheduleByDayUseCase` — no es
  necesario para cerrar la carrera en sí (el lock ya la cierra) y ensancharía el diff bastante más allá de
  lo que este finding pide. Queda como mejora futura, no como parte de este fix.
- **`UNIQUE KEY` sobre `availability (court_id, day_of_week)`** — evaluada y rechazada como parte de este
  loop (ver PLAN, Alternativa B) precisamente porque decidir si es correcta depende de la misma pregunta de
  producto que bloquea el `RELATED_FINDING` de arriba.
- **Cualquier otro finding de `docs/software-review-2026-08-31.md`** (F1, F2, F3, F5, F6–F15) — fuera de
  alcance de este loop por definición.

## Resultado final

**STATUS: RESOLVED**

El mecanismo descrito en F16 (`AddScheduleUseCase.execute()` sin lock, permitiendo que dos altas de
horario concurrentes para la misma cancha inserten dos filas de `availability` para el mismo `(court_id,
day_of_week)` y rompan con 500 toda consulta posterior de disponibilidad/reserva de esa cancha/día) está
cerrado. Evidencia concreta que lo sostiene:

- **Código:** `AddScheduleUseCase.execute()` toma `courtRepository.findByIdForUpdate(courtId)`
  (`@Lock(PESSIMISTIC_WRITE)`) como primera operación de la transacción — mismo patrón ya probado en
  `BookReservationUseCase`/`RescheduleReservationUseCase`/`DeleteCourtUseCase`/`DeleteBranchUseCase`.
- **Test de concurrencia real** (`AddScheduleConcurrencyTest`, Testcontainers/MySQL, `@RepeatedTest(5)`,
  10 hilos por repetición = 50 ejecuciones concurrentes en total): en las 5 repeticiones, exactamente 1
  ejecución tuvo éxito y las 9 restantes fallaron con `ScheduleAlreadyExistsException` (nunca con
  `IncorrectResultSizeDataAccessException` ni ninguna otra excepción); la base tuvo siempre exactamente 1
  fila para ese `(court, day)`; y `findByCourtIdAndDay` no rompió después de la carrera en ninguna
  repetición.
- **Test unitario nuevo** (`AddScheduleUseCaseTest`, 8 tests) — no existía ninguno antes de este loop;
  documenta el comportamiento funcional existente y el orden de invocación del lock nuevo.
- **Regresión:** suite completa, 270 tests, 0 failures, 0 errors, 0 skipped — incluye los 13 tests nuevos
  de este loop.
- **Scope:** diff acotado exactamente a `AddScheduleUseCase.java` (12 líneas) + los dos archivos de test
  nuevos — verificado con `git diff development --stat -- src pom.xml` en la fase `REVIEW`, no solo
  declarado.

Qué queda fuera de este loop:
- **`RELATED_FINDING`** (ver más abajo) — el gap secuencial de `filterConflicts()`, que permite que dos
  altas *no concurrentes* con rangos no solapados produzcan el mismo estado de dos filas por `(court,
  day)`. No tiene ID propio todavía; requiere una decisión de producto (¿horarios partidos por día son una
  funcionalidad querida?) antes de poder planearse. Recomendado: que un humano lo triage, y si corresponde,
  correr `software-review` para que reciba un ID estable y luego un loop propio.
- Los demás findings de `docs/software-review-2026-08-31.md` (F1–F15) — sin tocar, fuera de alcance de
  este loop por definición.
- `ReservationRepositoryAdapter.findActiveByCourtAndDay()` redirigiendo a la query muerta con lock (la
  otra mitad de F4) — sin tocar, finding distinto.
- No se abrió PR. Rama: `fix/f16-add-schedule-lock`, con 4 commits (ANALYZE, PLAN, IMPLEMENT+TEST, y este
  cierre) más el commit de fix en sí. Lista para que un humano la revise y decida abrir el PR.

## Related findings
- `RELATED_FINDING` (sin ID propio todavía — no viene de ningún reporte previo, se encontró durante el
  ANALYZE de este loop): **`filterConflicts()` en `AddScheduleUseCase.java:79-89` no impide, ni siquiera en
  ausencia total de concurrencia, que dos altas de horario *secuenciales* (dos requests HTTP separados, uno
  después de que el otro ya commiteó) inserten dos filas de `availability` para el mismo `(court_id,
  day_of_week)`, siempre que los rangos horarios no se solapen** (ej.: alta de "Lunes 8–12", y más tarde,
  sin ninguna carrera de por medio, alta de "Lunes 14–18" para la misma cancha). El chequeo solo rechaza
  solapamiento de horario, no la existencia de una fila previa para ese mismo día. Como
  `ScheduleRepositoryPort.findByCourtIdAndDay` (usado por `GetScheduleByDayUseCase`,
  `GetAvailableSlotsUseCase`, `BookReservationUseCase`, `RescheduleReservationUseCase`) asume que existe
  como mucho una fila por `(court, day)`, esta secuencia — sin ninguna carrera — produce el mismo síntoma
  de 500 que describe F16, por una causa distinta y más amplia (un gap de validación de negocio, no de
  concurrencia). Ver razonamiento completo en el ANALYZE de la Iteración 1. **Deliberadamente no se toca
  en este loop** — cerrarlo bien requiere una decisión de producto (¿las canchas pueden tener horarios
  partidos —mañana/tarde— el mismo día, o el invariante real es "un horario por cancha y día"?) que no le
  corresponde a este loop resolver. Queda para que un humano lo triage y, si corresponde, se audite con
  `software-review` para que reciba un ID propio.

---

## Iteración 1

### ANALYZE

**ANALYSIS RESULT**

- **Root cause:** `AddScheduleUseCase.execute()` (`AddScheduleUseCase.java:32-57`) hace un check-then-act
  sin lock: lee las canchas/horarios existentes con `scheduleRepository.findAllByCourtId(courtId)`
  (línea 52), decide en memoria de aplicación qué horarios nuevos no solapan con los existentes
  (`filterConflicts`, líneas 79-89), y recién después persiste con `scheduleRepository.saveAll(unique)`
  (línea 55). No hay ninguna operación que bloquee la fila de `Court` (ni ninguna otra) antes de esa
  lectura — a diferencia del resto de las mutaciones de este mismo dominio (ver "Existing protection"
  abajo). La tabla `availability` (`V1__baseline.sql:109-118`) no tiene ningún `UNIQUE` sobre
  `(court_id, day_of_week)`, solo un índice no-único (`idx_availability_court_day`).

- **Affected flow (re-trazado en el código actual — este branch se cortó de `development` @ `9a5e365`,
  idéntico al código auditado en `docs/software-review-2026-08-31.md`, confirmado con
  `git diff development fix/f1-login-rate-limiter-proxy-ip -- src pom.xml` vacío antes de cortar la
  rama):**
  - Entry point: `POST /api/schedules/court/{idCourt}` (`ScheduleController.java:30-42`), alcanzable por
    `hasRole('ADMIN')` o `@courtAuthorization.isOwnerOfCourt(#idCourt, authentication)` — el propio dueño
    de la cancha puede dispararlo, no hace falta un segundo actor.
  - `AddScheduleUseCase.execute()` → (carrera) → dos filas `ScheduleEntity` para el mismo
    `(court_id, day_of_week)` en `availability`.
  - `ScheduleRepository.findByCourtIdAndDay(long, DayOfWeek)` (`ScheduleRepository.java:26`) — derived
    query que devuelve `Optional<ScheduleEntity>`; Spring Data la genera esperando 0 o 1 resultado.
  - Consumida por `ScheduleGatewayAdapter.findByCourtAndDay()` (`ScheduleGatewayAdapter.java:18-21`) y
    directamente por `ScheduleRepositoryPort.findByCourtIdAndDay` a través de
    `GetScheduleByDayUseCase.execute()` (`GetScheduleByDayUseCase.java:16-19`).
  - Tres casos de uso más consumen la versión del gateway: `GetAvailableSlotsUseCase.execute()`
    (`GetAvailableSlotsUseCase.java:37`), `BookReservationUseCase.execute()`
    (`BookReservationUseCase.java:49`) y `RescheduleReservationUseCase.execute()`
    (`RescheduleReservationUseCase.java:77`).
  - Con 2 filas para el mismo `(court, day)`, cualquiera de esas cuatro llamadas dispara
    `IncorrectResultSizeDataAccessException` (JPA `NonUniqueResultException` por debajo) — no es
    `BusinessException` ni `DataIntegrityViolationException`, así que cae en
    `GlobalExceptionHandler.handleGenericException()` (`@ExceptionHandler(Exception.class)`) → **500**,
    con el stack trace solo en el log del servidor (`log.error("Unexpected error: {}", ...)`) y
    `"Ocurrió un error inesperado"` para el cliente.

- **Existing protection:** ninguna dentro de `AddScheduleUseCase`. El proyecto **ya tiene el patrón
  correcto para esta exacta clase de problema**, aplicado a los dos casos de uso hermanos en el mismo
  archivo/paquete: `UpdateScheduleUseCase.execute()` y `DeleteScheduleUseCase.execute()`, corregidos en la
  rama `fix/f4-schedule-concurrency-lock` (commit `06aa2cf`, **no mergeada a `development` todavía** —
  confirmado con `git log development..fix/f4-schedule-concurrency-lock`). Ambos toman
  `courtRepository.findByIdForUpdate(courtId)` (`CourtRepository.java:72-74`,
  `@Lock(PESSIMISTIC_WRITE)`) como **primera operación** de su método `@Transactional`, antes de leer
  cualquier otra cosa — el mismo lock que ya usan `BookReservationUseCase`, `RescheduleReservationUseCase`,
  `DeleteCourtUseCase` y `DeleteBranchUseCase`. `AddScheduleUseCase` quedó fuera de ese fix: `git show
  fix/f4-schedule-concurrency-lock --name-only` solo toca `UpdateScheduleUseCase.java` y
  `DeleteScheduleUseCase.java`, no `AddScheduleUseCase.java`.

- **Missing protection:** `AddScheduleUseCase.execute()` lee `Court` con
  `courtRepository.findById(courtId)` (línea 35-36) — `Optional<Court> findById(Long)`, sin lock — así que
  dos ejecuciones concurrentes para la misma cancha ven ambas el estado de `availability` previo a la
  carrera y pueden pasar `filterConflicts` en paralelo.

- **Confianza:** Confirmado. Mecanismo re-trazado end-to-end por lectura directa en esta misma pasada
  (no asumido del reporte de origen), sobre código verificado idéntico al auditado. Reachability
  confirmada: un solo actor (el Owner dueño de la cancha, o un Admin) puede disparar la carrera con un
  doble submit del mismo formulario — no requiere coordinar dos usuarios ni una ventana de negocio
  especial.

- **Hallazgo adicional durante este ANALYZE, registrado como `RELATED_FINDING` y explícitamente no
  tocado en este loop:** al trazar todos los consumidores de `findByCourtIdAndDay` para confirmar el
  "Affected flow", se verificó qué es lo único que impide que dos filas de `availability` coexistan para
  el mismo `(court, day)` — y la respuesta es: nada, ni siquiera en ausencia de concurrencia.
  `filterConflicts()` (`AddScheduleUseCase.java:79-89`) solo descarta un horario nuevo si **solapa en
  rango horario** con uno existente del mismo día (`overlaps()`, líneas 91-93) — no rechaza un horario
  nuevo simplemente porque el día ya tiene una fila. Dos requests HTTP completamente secuenciales — el
  segundo disparado bastante después de que el primero ya commiteó, sin ninguna carrera — con rangos que
  no se solapan (ej. "Lunes 8–12" y, en otra request posterior, "Lunes 14–18") pasan ambos el chequeo y
  producen igualmente dos filas para el mismo `(court, day)`, con la misma consecuencia (`findByCourtIdAndDay`
  rompe con 500 la próxima vez que alguien consulte disponibilidad ese día). Verificado que ningún otro
  caso de uso ni test en el repo (`GetAllSchedulesByCourtUseCase`, `GetScheduleByDayUseCase`,
  `ConcurrencyTestFixtures`, `ScheduleControllerTest`) ejercita ni documenta "horarios partidos" (mañana/
  tarde) como un escenario soportado — tampoco hay ningún comentario o test que lo contradiga
  explícitamente. Esto es ambiguo por diseño, no por falta de investigación: **cerrarlo bien requiere saber
  si "más de un bloque horario por cancha y día" es una funcionalidad querida** (en cuyo caso el bug real
  está en el lado de lectura, que asume `Optional` de a una fila) **o si el invariante real es "un
  horario por cancha y día"** (en cuyo caso el bug está en `filterConflicts`, que debería rechazar
  cualquier horario nuevo para un día que ya tiene uno, no solo los que solapan). Ninguna lectura de
  código puede resolver esa pregunta — es la condición de parada de ANALYZE para *ese* mecanismo
  específico, aplicada de forma acotada: no bloquea el mecanismo de F16 (la carrera), que sí tiene una
  corrección clara y sin ambigüedad de producto (ver PLAN).

**Decisión de fase:** el mecanismo de F16 (la carrera) está trazado con evidencia concreta y alcanzable, y
tiene un patrón de corrección ya probado en este mismo código para los dos casos de uso hermanos. Se
avanza a `PLAN`. El mecanismo adicional descubierto (gap secuencial de `filterConflicts`) se deja
registrado arriba como `RELATED_FINDING` y no participa de `PLAN`/`IMPLEMENT` de este loop.

### PLAN

**PLAN RESULT**

**Alternativas comparadas:**

- **A — Replicar el lock pesimista ya usado en el resto del dominio (elegida).** Cambiar la primera
  lectura de `AddScheduleUseCase.execute()` de `courtRepository.findById(courtId)` a
  `courtRepository.findByIdForUpdate(courtId)` — método que ya existe en `CourtRepositoryPort`
  (`CourtRepositoryPort.java:21`) y ya está implementado con `@Lock(PESSIMISTIC_WRITE)`
  (`CourtRepository.java:72-74`), sin tocar nada más del método. Dos ejecuciones concurrentes de
  `AddScheduleUseCase.execute()` para la misma cancha quedan serializadas por la fila de `Court`: la
  segunda transacción no puede leer `availability` hasta que la primera commitea, así que ve las filas ya
  insertadas por la primera y `filterConflicts()` las detecta correctamente como conflicto. Es exactamente
  el mismo patrón que ya cierran `BookReservationUseCase`, `RescheduleReservationUseCase`,
  `DeleteCourtUseCase`, `DeleteBranchUseCase`, y — sobre el mismo archivo hermano — `UpdateScheduleUseCase`/
  `DeleteScheduleUseCase` en `fix/f4-schedule-concurrency-lock`. Como beneficio colateral (no el objetivo
  de este fix, pero verificado que no rompe nada): al competir por la misma fila de `Court`, esto también
  serializa `AddScheduleUseCase` contra `Book`/`Update`/`Delete` de esa cancha, consistente con cómo ya se
  comportan entre sí las demás mutaciones de este dominio.
  - **Trade-off:** no agrega ningún respaldo a nivel de base — si una fila duplicada ya existe en la base
    (por ejecuciones anteriores al fix, o por el gap secuencial fuera de alcance), el lock no la limpia ni
    evita que siga rompiendo lecturas para ese `(court, day)` puntual. Aceptado: el finding pide cerrar la
    carrera, no migrar datos existentes ni agregar tolerancia de lectura — ver `OUT_OF_SCOPE`.

- **B — `UNIQUE KEY` en `availability (court_id, day_of_week)`, sin lock de aplicación.** Cerraría la
  carrera a nivel de base (la segunda inserción concurrente fallaría con
  `DataIntegrityViolationException` → 409, manejado con gracia por `GlobalExceptionHandler`). **Rechazada**
  por dos razones concretas, no solo preferencia: (1) es exactamente la misma pregunta de producto que
  bloquea el `RELATED_FINDING` — el código de `filterConflicts()` está escrito para permitir explícitamente
  más de un horario por día si no solapan (lógica de solapamiento, no de igualdad de día), y agregar esta
  constraint sin resolver esa ambigüedad rompería a nivel de base cualquier intento futuro de horario
  partido, incluso uno legítimo, con un 409 genérico que no explica la causa real; decidir eso no es parte
  de este finding. (2) aunque se resolviera esa pregunta a favor de "un horario por día", una `UNIQUE`
  constraint sola seguiría dejando la carrera cerrarse con una excepción no ideal (`DataIntegrityViolationException`
  genérica en vez de `ScheduleAlreadyExistsException`, que es la que ya usa este mismo caso de uso para
  el mismo escenario en el camino sin carrera) — el lock, en cambio, hace que el segundo hilo pase por el
  mismo `filterConflicts()` que ya lanza la excepción de negocio correcta. Se deja documentada como
  alternativa futura condicionada a resolver el `RELATED_FINDING`, no como parte de este fix.

- **C — Optimistic locking (columna `@Version` en `ScheduleEntity`/`CourtEntity`).** Rechazada sin mayor
  desarrollo: el proyecto no usa este patrón en ningún otro lugar (todo el resto de la concurrencia del
  dominio usa lock pesimista), introduciría una segunda estrategia de concurrencia a mantener y explicar,
  y el caso de uso (bloquear brevemente durante un alta administrativa poco frecuente, no un flujo de alto
  volumen) es precisamente el caso para el que el lock pesimista ya elegido en este proyecto es la
  herramienta correcta — no hay contención suficientemente alta como para que el costo del lock pesimista
  importe.

**Evidencia requerida antes de poder decir `RESOLVED`:** dado que el riesgo de F16 es específicamente de
**concurrencia real** (no una carrera "de juguete"), un test unitario con Mockito que solo verifique el
orden de las llamadas (`findByIdForUpdate` antes que `findAllByCourtId`) **no alcanza por sí solo** — es
exactamente la distinción que ya hace `BookReservationConcurrencyTest` en su propio javadoc. Se requiere:
1. Un test unitario (Mockito) que confirme el orden de invocación — barato, rápido, sirve de documentación
   ejecutable del contrato.
2. Un test de concurrencia real contra **MySQL vía Testcontainers** (no H2 — H2 no reproduce fielmente
   `PESSIMISTIC_WRITE`/`SELECT ... FOR UPDATE`, mismo razonamiento que ya documenta
   `BookReservationConcurrencyTest`) que lance N hilos concurrentes ejecutando
   `AddScheduleUseCase.execute()` para la misma cancha con rangos horarios que se solapan entre sí, y
   verifique — contra la base real, no solo contra lo que devolvió cada hilo — que como máximo una
   ejecución concurrente por conjunto de horarios solapados haya insertado su fila, que el resto haya
   fallado con `ScheduleAlreadyExistsException` (no con ninguna otra excepción, y en particular no con
   `IncorrectResultSizeDataAccessException`), y que una llamada posterior a `findByCourtIdAndDay` para esa
   cancha/día no rompa.
3. Ejecución real de ambos (no "deberían pasar") — ver fase `TEST`.

**Scope:** ver sección `## Scope` arriba (`IN_SCOPE`/`OUT_OF_SCOPE`), completada como parte de este PLAN.

**Decisión de fase:** alternativa A elegida con trade-offs explícitos, scope concreto, evidencia requerida
específica y verificable. Se avanza a `IMPLEMENT`.

### IMPLEMENT

**IMPLEMENTATION RESULT**

**Archivos tocados (`git diff --stat` contra `development`):**
- `src/main/java/com/deportlink/deportlink/application/usecase/schedule/AddScheduleUseCase.java` — 11
  inserciones, 1 eliminación. Cambio real: `courtRepository.findById(courtId)` →
  `courtRepository.findByIdForUpdate(courtId)` como primera lectura de `execute()`, con un comentario
  explicando el porqué (mismo estilo que el comentario ya presente en `UpdateScheduleUseCase` de la rama
  `fix/f4-schedule-concurrency-lock`). Ninguna otra línea del método cambió — mismo orden de las
  validaciones subsiguientes (`isActive`, `branch.isApproved`, `validateTimeRanges`, `filterConflicts`,
  `saveAll`).
- `src/test/java/com/deportlink/deportlink/usecase/schedule/AddScheduleUseCaseTest.java` (nuevo, 8 tests)
  — no existía ningún test unitario para este caso de uso. Cubre: alta sin conflictos, alta que solapa con
  un horario existente (`ScheduleAlreadyExistsException`), rango horario inválido, cancha inactiva,
  sucursal no aprobada, sucursal no encontrada, cancha no encontrada (verifica que el lock se toma ANTES
  de tocar sucursal/agenda), y el orden de invocación `findByIdForUpdate` → `findAllByCourtId` → `saveAll`.
- `src/test/java/com/deportlink/deportlink/usecase/reservation/AddScheduleConcurrencyTest.java` (nuevo, 1
  test `@RepeatedTest(5)`) — Testcontainers/MySQL real, 10 hilos concurrentes dando de alta el mismo rango
  horario para la misma cancha/día; verifica que gana exactamente uno, que el resto falla con
  `ScheduleAlreadyExistsException` (no con `IncorrectResultSizeDataAccessException` ni ninguna otra), que
  la base tiene exactamente una fila para ese `(court, day)`, y que `findByCourtIdAndDay` no rompe después.

**Comparación contra `IN_SCOPE`:** los tres archivos coinciden exactamente con lo declarado en `## Scope`
al final de `PLAN`. No se tocó ningún archivo fuera de esa lista — en particular, no se tocó
`ConcurrencyTestFixtures.java` (compartida con otros tests ya en verde): el gap que esa fixture tiene para
este caso de uso (no fija `activeStatus`/`verificationStatus`, porque ningún otro test de esa familia los
necesita) se resolvió activando la cancha/sucursal directamente dentro de `AddScheduleConcurrencyTest`,
sin modificar el archivo compartido — ver más abajo.

**Desviación encontrada y resuelta durante la implementación (no un cambio de scope, un ajuste dentro del
archivo de test nuevo):** la primera ejecución de `AddScheduleConcurrencyTest` falló — los 10 hilos
fallaban con `ClubNotActivedException`, no con el resultado esperado. Investigado antes de tocar nada más:
`ConcurrencyTestFixtures.createBranchWithCourts()` nunca fija `CourtEntity.activeStatus` ni
`BranchEntity.verificationStatus`/`activeStatus` (quedan `null` por default de Java, sin anotación de
valor por defecto en la entidad) — ningún otro test de esta familia (`Book`/`Reschedule`/`Delete*`) llama a
`Court.isActive()` ni a `Branch.isApproved()`, así que nunca hizo falta. `AddScheduleUseCase` sí los
chequea (líneas ya existentes, sin tocar) antes de llegar a `filterConflicts()`. Corregido dentro del
`@BeforeEach` de `AddScheduleConcurrencyTest`: después de crear la fixture, se carga la `BranchEntity`/
`CourtEntity` recién creada y se fija `APPROVED`/`ACTIVE` explícitamente vía los repositorios ya
autowireados. No requirió tocar `AddScheduleUseCase.java` ni la fixture compartida — se mantiene dentro
de `IN_SCOPE` (el archivo de test nuevo).

**Compila:** `./mvnw -o compile test-compile` — sin errores, dos veces (antes y después del ajuste de
`activeStatus`/`verificationStatus` de arriba).

**Decisión de fase:** implementación completa, coincide con el plan, sin desviaciones fuera de `IN_SCOPE`.
Se avanza a `TEST`.

### TEST

**Entorno verificado antes de correr nada:** `docker info` — Docker disponible (confirmado con éxito antes
de intentar el test de Testcontainers, no asumido).

**Lo que corrió, cuántas veces, y resultado:**

1. `AddScheduleUseCaseTest` (Mockito, sin infraestructura) — `./mvnw -o test -Dtest=AddScheduleUseCaseTest`
   → **8/8 tests, 0 failures, 0 errors** (`target/surefire-reports/com.deportlink.deportlink.usecase.schedule.AddScheduleUseCaseTest.txt`).
2. `AddScheduleConcurrencyTest` (Testcontainers/MySQL real) — primera corrida:
   `./mvnw -o test -Dtest=AddScheduleConcurrencyTest` → **5/5 repeticiones fallaron**, las 10 ejecuciones
   concurrentes de cada repetición fallando con algo distinto de `ScheduleAlreadyExistsException`. Reportado
   completo, no resumido: se instrumentó temporalmente el test para loguear la clase real de cada falla —
   `com.deportlink.deportlink.exception.ClubNotActivedException: "No puede agregar agenda, porque la cancha
   no se encuentra activa"` en las 10 ejecuciones de cada repetición. **No era un defecto del fix ni del
   mecanismo de lock** — era un gap de la fixture compartida (ver IMPLEMENT). Corregido dentro del test
   (activar cancha/sucursal explícitamente), no dentro del código de producción ni de la fixture
   compartida.
3. `AddScheduleConcurrencyTest`, segunda corrida tras el ajuste — **5/5 repeticiones, 0 failures, 0
   errors**, 55.71s (`target/surefire-reports/com.deportlink.deportlink.usecase.reservation.AddScheduleConcurrencyTest.txt`).
   Se sacó el logging temporal de diagnóstico del archivo de test antes de este punto.
4. Corrida final limpia de ambos (`AddScheduleUseCaseTest,AddScheduleConcurrencyTest`) tras sacar el
   debug — **8/8 y 5/5, 0 failures, 0 errors, 60.21s** — confirma que sacar el logging no rompió nada.
5. **Regresión — suite completa** (`./mvnw -o test`, corrida en background por el tiempo que toma):
   resultado documentado abajo una vez terminó.

**Qué no pudo correr:** nada — Docker estaba disponible, no hizo falta ninguna reanudación de `TEST`.

**Resultado de la regresión (suite completa, `./mvnw -o test`):** terminó con exit code 0. Verificado no
solo por el exit code — agregado el conteo real de `target/surefire-reports/*.txt` (40 archivos de
reporte, uno por clase de test): **270 tests, 0 failures, 0 errors, 0 skipped** en total, incluyendo los
dos archivos nuevos de este loop (`AddScheduleUseCaseTest.txt`, `AddScheduleConcurrencyTest.txt`
confirmados presentes en la lista de reportes). Sin regresiones: ninguna clase de test pre-existente
falló.

**Decisión de fase:** los tres puntos de evidencia requerida por `PLAN` están cubiertos y ejecutados de
verdad (no "deberían pasar"): test unitario del orden de invocación (verde), test de concurrencia real
contra MySQL (verde, incluyendo la verificación explícita de que `findByCourtIdAndDay` no rompe después
de la carrera), y regresión de la suite completa (verde, sin excepciones). Se avanza a `REVIEW`.

### REVIEW

**REVIEW RESULT** — re-derivado desde cero contra el código y el diff actuales, no contra el resumen de
`IMPLEMENT`/`TEST` de arriba.

1. **¿Se cerró el mecanismo real del finding?** Re-leído `AddScheduleUseCase.java` completo en su estado
   actual (no el diff, el archivo entero): la primera línea de `execute()` es
   `courtRepository.findByIdForUpdate(courtId)`; todo el resto del método (validaciones de estado,
   `filterConflicts`, `saveAll`) sigue exactamente igual. Re-confirmado en `CourtRepository.java:72-74`
   que `findByIdForUpdate` sigue siendo `@Lock(PESSIMISTIC_WRITE)`. El mecanismo que `ANALYZE` trazó (dos
   transacciones concurrentes leyendo `availability` antes de que cualquiera inserte) ya no es posible:
   la segunda transacción no puede ejecutar su `SELECT ... FOR UPDATE` sobre la misma fila de `Court`
   hasta que la primera libere el lock al commitear — confirmado no solo por lectura del código sino
   empíricamente por el log de Hibernate de la corrida real (`select ... from court ... for update`
   repetido, seguido de los inserts serializados) y por el resultado del test (exactamente 1 éxito en
   las 5 repeticiones de 10 hilos).
2. **¿La evidencia que produjo `TEST` es del tipo que el riesgo de F16 exige, o un sustituto más barato?**
   Es del tipo correcto — no se sustituyó una prueba de concurrencia real por unit tests con Mockito
   (que solo prueban orden de invocación, no que el lock serialice). El test de Testcontainers corre
   contra MySQL real (no H2), con el mismo patrón ya validado en `BookReservationConcurrencyTest`.
   Verificado releyendo el archivo de test completo en esta pasada (no solo el resumen de `TEST`): usa
   `CountDownLatch` para largar los 10 hilos lo más simultáneamente posible, y valida contra la base real
   (`scheduleRepository.findByCourtId`), no solo contra lo que devolvió cada hilo — mismo estándar que
   `BookReservationConcurrencyTest`.
3. **¿Regresiones?** Ninguna. Re-confirmado contando `target/surefire-reports/*.txt` de la corrida
   completa en esta misma pasada (no reutilizando el conteo de `TEST` sin volver a mirarlo): 40 archivos
   de reporte, 270 tests, 0 failures, 0 errors, 0 skipped.
4. **¿El diff real está completamente dentro de `IN_SCOPE`?** Re-ejecutado
   `git diff development --stat -- src pom.xml` en esta pasada (no confiado en lo que dijo `IMPLEMENT`):
   3 archivos — `AddScheduleUseCase.java` (12 líneas), `AddScheduleConcurrencyTest.java` (nuevo, 215
   líneas), `AddScheduleUseCaseTest.java` (nuevo, 184 líneas). Los tres están en `IN_SCOPE` tal cual se
   declaró al final de `PLAN`. Ningún archivo fuera de esa lista.
5. **¿Algún archivo modificado que no sea parte de este finding?** No — mismo resultado que el check 4.
   `ConcurrencyTestFixtures.java` (compartida) **no** aparece en el diff — se verificó explícitamente con
   `git diff development --stat` que no está en la lista, confirmando que el ajuste de
   `activeStatus`/`verificationStatus` documentado en `IMPLEMENT` efectivamente se resolvió dentro del
   archivo de test nuevo y no tocó el archivo compartido.
6. **¿Queda algo pendiente que el plan haya pedido?** No. Las tres piezas de evidencia que `PLAN` exigió
   antes de poder decir `RESOLVED` están presentes y verdes: test unitario de orden, test de concurrencia
   real con las cuatro aserciones específicas que pedía `PLAN` (≤1 éxito, el resto
   `ScheduleAlreadyExistsException` y no otra cosa, exactamente 1 fila en la base, `findByCourtIdAndDay`
   no rompe después), y regresión de la suite completa.

**Búsqueda activa de algo que contradiga el hallazgo (no solo confirmarlo):** se reconsideró si el lock
sobre `Court` podría no ser suficiente — por ejemplo, si alguna ruta insertara en `availability` sin pasar
por `AddScheduleUseCase.execute()` (lo que dejaría el lock sin efecto para esa ruta). Grep de
`scheduleRepository.save` y `.saveAll(` en `src/main/java` confirma que `AddScheduleUseCase` es el único
punto de escritura de `availability` (`UpdateScheduleUseCase`/`DeleteScheduleUseCase` modifican una fila
existente por id o la eliminan, no crean filas nuevas) — no hay una segunda vía de inserción que el lock
no cubra. También se reconsideró si el aislamiento por defecto de MySQL (`REPEATABLE READ`) podría hacer
que la segunda transacción, aun serializada, leyera un snapshot *anterior* al commit de la primera — no es
así para lecturas con lock (`SELECT ... FOR UPDATE` lee la versión más reciente comprometida, no el
snapshot de la transacción), consistente con el resultado empírico del test (0 casos de doble inserción en
50 ejecuciones concurrentes totales — 5 repeticiones × 10 hilos). Ninguna de las dos búsquedas encontró
algo que contradiga el hallazgo.

**Sobre el `RELATED_FINDING` (gap secuencial de `filterConflicts`):** re-confirmado en esta pasada que
sigue existiendo, sin cambios, y que efectivamente queda fuera de lo que este fix cierra — no se intentó
cerrarlo ni se dejó pasar como si este fix lo cubriera. El README del loop y el commit lo declaran
explícitamente como fuera de alcance.

**Veredicto:** evidencia suficiente. Nada pendiente. Sin regresiones. Diff completo dentro de `IN_SCOPE`.

**Decisión de fase:** se avanza a `DECIDE`.

### DECIDE

- **Veredicto:** evidencia suficiente (los 6 checks de `REVIEW` cierran sin nada pendiente).
- **Ruta:** `RESOLVED`.
- **Razón:** el mecanismo de F16 (carrera sin lock en `AddScheduleUseCase`, permitiendo dos filas de
  `availability` para el mismo `(court_id, day_of_week)` y rompiendo con 500 la disponibilidad/reserva de
  esa cancha/día) está cerrado por el lock pesimista sobre `Court`, demostrado con evidencia ejecutada
  (no asumida): test de concurrencia real contra MySQL (Testcontainers) con 5 repeticiones de 10 hilos
  concurrentes, 0 casos de doble inserción, y confirmación explícita de que la consulta que antes rompía
  (`findByCourtIdAndDay`) ya no lo hace después de la carrera. Regresión de la suite completa (270 tests)
  sin fallas. Diff acotado exactamente a lo declarado en `IN_SCOPE`.
